version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=mongodb+srv://Vercel-Admin-atlas-fuchsia-lamp:YvoKsbPWnErspVyb@atlas-fuchsia-lamp.voqfqxg.mongodb.net/?retryWrites=true&w=majority
      - JWT_SECRET=e59adccec84a8ababb1d97f64719deea966e622848deb24891af712ea0225007
      - JWT_EXPIRE=7d

  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    ports:
      - "8080:80"
    depends_on:
      - server